const Joi = require("joi");
const objectId = Joi.string().hex().length(24);
const scoringConfig = Joi.object({
  skillWeight: Joi.number().min(0).max(1).required(), experienceWeight: Joi.number().min(0).max(1).required(), stabilityWeight: Joi.number().min(0).max(1).required(),
}).custom((value, helpers) => Math.abs(value.skillWeight + value.experienceWeight + value.stabilityWeight - 1) < 0.001 ? value : helpers.error("any.invalid"));
const jobBody = Joi.object({
  type: Joi.string().valid("gig", "full_time").required(), title: Joi.string().min(3).max(200).required(), description: Joi.string().min(20).max(20000).required(),
  scoringConfig: scoringConfig.required(), minResumePct: Joi.number().min(0).max(100), minTestPct: Joi.number().min(0).max(100), cooldownDays: Joi.number().integer().min(0).max(365),
  gigDetails: Joi.when("type", { is: "gig", then: Joi.object({ budget: Joi.number().positive().required(), deadline: Joi.date().required(), deliverables: Joi.array().items(Joi.string()), skillsRequired: Joi.array().items(Joi.string()).min(1).required(), durationEstimate: Joi.string() }).required(), otherwise: Joi.forbidden() }),
  fullTimeDetails: Joi.when("type", { is: "full_time", then: Joi.object({ salaryMin: Joi.number().min(0).required(), salaryMax: Joi.number().min(Joi.ref("salaryMin")).required(), location: Joi.string().required(), workMode: Joi.string().valid("remote", "onsite", "hybrid").required(), department: Joi.string(), employmentType: Joi.string().valid("contract", "permanent").required(), experienceRequiredYears: Joi.number().min(0).required(), jdDocumentUrl: Joi.string().uri() }).required(), otherwise: Joi.forbidden() }),
});
module.exports = {
  createJob: { body: jobBody }, updateJob: { params: Joi.object({ id: objectId.required() }), body: jobBody.fork(["type", "title", "description", "scoringConfig"], (schema) => schema.optional()) },
  id: { params: Joi.object({ id: objectId.required() }) }, listJobs: { query: Joi.object({ type: Joi.string().valid("gig", "full_time"), finalStatus: Joi.string(), flaggedForReview: Joi.boolean(), minScore: Joi.number().min(0).max(100), maxScore: Joi.number().min(0).max(100), tags: Joi.string(), sort: Joi.string().valid("newest", "score"), page: Joi.number().integer().min(1), limit: Joi.number().integer().min(1).max(100) }) },
  createTest: { params: Joi.object({ id: objectId.required() }), body: Joi.object({ type: Joi.string().valid("mcq", "task").required(), timeLimitMinutes: Joi.number().integer().min(1).required(), proctoringEnabled: Joi.boolean(), maxAttempts: Joi.number().integer().min(1).max(5), questions: Joi.array().items(Joi.object({ prompt: Joi.string().required(), options: Joi.array().items(Joi.string()), correctAnswer: Joi.string(), taskSpec: Joi.string(), rubric: Joi.array().items(Joi.string()) })).min(1).required() }) },
  apply: { params: Joi.object({ id: objectId.required() }), body: Joi.object({ resumeId: objectId, resumeUrl: Joi.string().uri().allow(""), resumeText: Joi.string().min(50).max(100000).when("resumeId", { is: Joi.exist(), then: Joi.optional(), otherwise: Joi.required() }) }) },
  submit: { params: Joi.object({ id: objectId.required() }), body: Joi.object({ answers: Joi.array().items(Joi.object({ questionId: objectId.required(), response: Joi.string().allow("").required() })).default([]), proctoringFlags: Joi.array().items(Joi.string().valid("tab_switch", "paste_detected", "fullscreen_exit")) }) },
  lifecycle: { params: Joi.object({ id: objectId.required() }), body: Joi.object({ reason: Joi.string().trim().max(500), publishAt: Joi.date().greater("now"), expiresAt: Joi.date().greater("now") }) },
  schedule: { params: Joi.object({ id: objectId.required() }), body: Joi.object({ reason: Joi.string().trim().max(500), publishAt: Joi.date().greater("now").required(), expiresAt: Joi.date().greater(Joi.ref("publishAt")) }) },
  review: { params: Joi.object({ id: objectId.required() }), body: Joi.object({ finalStatus: Joi.string().valid("under_review", "interview_requested", "shortlisted", "hired", "rejected").required(), reason: Joi.string().trim().max(1000).when("finalStatus", { is: "rejected", then: Joi.required() }) }) },
  withdraw: { params: Joi.object({ id: objectId.required() }), body: Joi.object({ reason: Joi.string().trim().max(500) }) },
  management: { params: Joi.object({ id: objectId.required() }), body: Joi.object({ posterNotes: Joi.string().trim().max(5000).allow(""), tags: Joi.array().items(Joi.string().trim().max(50)).max(20) }).min(1) },
  scoreOverride: { params: Joi.object({ id: objectId.required() }), body: Joi.object({ weighted: Joi.number().min(0).max(100).required(), reason: Joi.string().trim().min(10).max(1000).required() }) },
  manualTaskScore: { params: Joi.object({ id: objectId.required() }), body: Joi.object({ score: Joi.number().min(0).max(100).required(), justification: Joi.string().trim().min(10).max(2000).required() }) },
  proposeInterview: { params: Joi.object({ id: objectId.required() }), body: Joi.object({ startsAt: Joi.date().greater("now").required(), endsAt: Joi.date().greater(Joi.ref("startsAt")).required(), timezone: Joi.string().trim().max(100).required(), meetingLink: Joi.string().uri().allow("") }) },
  interviewResponse: { params: Joi.object({ id: objectId.required() }), body: Joi.object({ action: Joi.string().valid("confirmed", "reschedule_requested").required(), reason: Joi.string().trim().max(1000).when("action", { is: "reschedule_requested", then: Joi.required() }) }) },
  interviewUpdate: { params: Joi.object({ id: objectId.required() }), body: Joi.object({ startsAt: Joi.date().greater("now"), endsAt: Joi.date().greater(Joi.ref("startsAt")), timezone: Joi.string().trim().max(100), meetingLink: Joi.string().uri().allow(""), action: Joi.string().valid("completed", "cancelled"), reason: Joi.string().trim().max(1000).when("action", { is: "cancelled", then: Joi.required() }), privateNotes: Joi.string().max(10000).allow(""), scorecard: Joi.array().items(Joi.object({ category: Joi.string().trim().max(100).required(), score: Joi.number().integer().min(1).max(5).required(), note: Joi.string().trim().max(1000).allow("") })).max(20) }).min(1) },
  bulkReview: { params: Joi.object({ id: objectId.required() }), body: Joi.object({ applicationIds: Joi.array().items(objectId.required()).min(1).max(100).required(), finalStatus: Joi.string().valid("shortlisted", "rejected").required(), reason: Joi.string().trim().max(1000).when("finalStatus", { is: "rejected", then: Joi.required() }) }) },
};
